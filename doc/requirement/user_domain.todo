//TODO: Create User usecase
//[ ]: validate input correctly (fail-fast)
//[ ]: validate user existence by email duplicateted entry
//[ ]: save user data model in cache with key user_activation_{activation code}
//[ ]: UserCriationStarded event must be emited (Payload: activation code, user first name, user email)
//[ ]: UserCriationWithCouponStarded event must be emited if there is coupon in body (Payload: coupon code and activation code)

//TODO: Create User confirmation
//[ ]: validate inout correctly (fail-fast)
//[ ]: get user data in cache with key user_activation_{activation code}
//[ ]: include defer after above item in cache with key user_activation_{activation code}
//[ ]: parse user to data model
//[ ]: create User entity
//[ ]: invoke user domain service to validate category_id and country_id (use repository to ensure)
//[ ]: parse User entity to userDto and save in db
//[ ]: UserWithCouponConfirmed event must be emited if there is coupon in input (Payload: activation_code and user_id)
//[ ]: Create session channel in map and emit UserCreated event with (Payload: user id). Waiting for channel result to return